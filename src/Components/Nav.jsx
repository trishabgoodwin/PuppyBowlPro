import { Routes, Route, Link } from "react-router-dom";

function Nav() {
    return (
      <footer id="navbar">
          <Link to="/">Home</Link>
      </footer>
    );
  }
  
  export default Nav;